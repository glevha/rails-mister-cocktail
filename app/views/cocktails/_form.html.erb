<%= simple_form_for(cocktail) do |form| %>
	<%= form.error_notification %>

	<%= form.input :name %>
  <%= form.button :submit, value: "Fus' drink!", class: "btn-medium-pink", data: { disable_with: "Fus' drink!" }%>
<% end %>